- if flash[:notice]
    = flash[:notice]

= @post.title
= @post.content
ASDF
= render @post.comments

= render 'comments/form'



= link_to "Destroy Item", post_path(@post), method: :delete, data: {confirm: "Are you sure you want to delete this post?"}